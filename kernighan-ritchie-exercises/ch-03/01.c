/*
 * Exercise 3-1: Our binary search makes two tests inside the loop, when one would suffice (at
 * the price of more tests outside). Rewrite it with only one test inside the
 * loop and measure the difference in speed.
 */

#include <stdio.h>

int main(void) {
  fprintf(stderr, "PENDING\n");
  return 0;
}

