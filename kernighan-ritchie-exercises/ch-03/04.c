/*
 * Exercise 3-4: In a two's complement number representation, our version of itoa does not
 * handle the largest negative number, which is the number with only the sign
 * bit set. Explain why, and fix the defect.
 */

#include <stdio.h>

int main(void) {
  fprintf(stderr, "PENDING\n");
  return 0;
}

