/*
 * Exercise 2-7: Write a function invert(x,p,n) that returns x with the n bits that begin at
 * position p inverted (1 changed to 0 and vice versa).
 */

#include <stdio.h>

int main(void) {
  fprintf(stderr, "PENDING\n");
  return 0;
}

