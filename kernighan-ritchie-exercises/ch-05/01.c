/*
 * Exercise 5-1: As written, getint treats a + or - not followed by a digit as a valid
 * representation of zero. Fix it to push the sign back on the input.
 */

#include <stdio.h>

int main(void) {
  fprintf(stderr, "PENDING\n");
  return 0;
}

