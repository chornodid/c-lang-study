/*
 * Exercise 8-4: The standard library function int fseek(FILE *fp, long offset, int origin) is
 * identical to lseek except that fseek returns 0 for OK and -1 for errors.
 * Implement fseek in terms of lseek.
 */

#include <stdio.h>

int main(void) {
  fprintf(stderr, "PENDING\n");
  return 0;
}

