/*
 * Exercise 8-6: The standard library function calloc(n,size) allocates memory
 * for an array of n objects of size size, with the memory initialized to zero.
 * Write calloc, by calling malloc or by modifying it.
 */

#include <stdio.h>

int main(void) {
  fprintf(stderr, "PENDING\n");
  return 0;
}

