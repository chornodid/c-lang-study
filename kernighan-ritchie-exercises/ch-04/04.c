/*
 * Exercise 4-4: Add commands to print the top element of the stack without popping, to
 * duplicate it, and to swap the top two elements.
 */

#include <stdio.h>

int main(void) {
  fprintf(stderr, "PENDING\n");
  return 0;
}

